// exercise solutions..
let userInput = '42'; // 42 is string
num = Number(userInput); // convert str input to number
alert(num + 8); // 50

let emptyInput = ''; // empty input
bol = Boolean(emptyInput); // converted emptyinput in boolean
alert(bol); // false.

let randomValue = null; // random value input.
alert(Number(randomValue)); // 0

let numAndStr = "123abc"; // its a var value is string for now
numOnly = Number(numAndStr); // converting str to num using Number function
alert(numOnly); // NaN not a number because that input caries alphabets.

