let numAge = 23; // var storing a number
let strName = 'Hamza'; // var storing string
let boolIsAdult = true; // var storing a boolean
alert(typeof boolIsAdult); // Boolean type check
alert(typeof strName); // string type check
alert(typeof numAge); // number Type check

// completed

let numA = 10; //  var storing a number
let strB = '5'; // var storing a string
alert('string concatenation ' + numA + strB); // contcatention
convert = Number(strB); // convert str to num
alert(numA - convert); // substract.. result: 5

// completed

let strNum = '123'; // string 
let conv = Number(strNum); // convert string to number
alert(conv + 7); // simple plus to get result 130

// completed

let bolZero = 0; // define a var with 0; num in value
direct = Boolean(bolZero); // convert num in to a boolean
alert(direct); // false' 

// completed

let str56 = "   56  "; // str with tabs
toNum = Number(str56); // converted to number
alert(toNum); // result 56 no spaceds


// completed.

let convertHello = 'hello123' // var contains num and str
alert(Number(convertHello)); // NaN : Not a Number.


// Finished
